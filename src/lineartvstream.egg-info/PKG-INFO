Metadata-Version: 2.4
Name: lineartvstream
Version: 0.2.0
Summary: Streaming server that recreates linear TV experience
Requires-Python: <4.0,>=3.12
Description-Content-Type: text/markdown
Requires-Dist: colorama==0.4.6
Requires-Dist: Flask==3.1.1
Requires-Dist: hypermedia==5.4.0
Requires-Dist: isodate==0.7.2
Requires-Dist: MarkupSafe==3.0.2
Requires-Dist: pydantic==2.12.5
Requires-Dist: pydantic_core==2.41.5
Requires-Dist: python-dotenv==1.2.1
Requires-Dist: python-slugify==8.0.4
Requires-Dist: requests==2.32.5
Requires-Dist: SQLAlchemy==2.0.43
Requires-Dist: tmdbsimple==2.9.1
Requires-Dist: yt-dlp==2026.1.29.165626.dev0

**Features**

- Fixed weekly broadcast schedule for series and movies
- Automatic content downloading via yt-dlp
- Metadata integration from TMDB
- Admin panel for schedule management

**Guide**

1. Initialization
```
pip install https://github.com/Stalldyr/LinearTV.git
```

2. Schedule administration and testing

Administrate and test the viewing schedule by running:
```
from lineartvstream.stream_app import stream_app
python stream_app.py
```

Or in order to access the streamer from another Flask-application: 
```
from lineartvstream.stream_app import stream_app

app = Flask(__name__)
app.register_blueprint(stream_app)
```
Then in terminal:
```
# Start server
python app.py
# Or in production:
gunicorn -c gunicorn_config.py app:app
```


If you want the streaming without the routes you could import the html:
```
from lineartvstream.ui.stream_html import stream_html
html = stream_html().dump()
```


3. Schedule maintain

Currently there is no UI to maintain the schedule, this has to be done through the `tvstreamer-prep` command. It's recommended to run this periodically via cron — how often is up to you (e.g. `sync` daily, `refresh` weekly).

\```
# Fetch upcoming NRK schedule data
tvstreamer-prep fetch

# Enrich episodes/movies with metadata (yt-dlp / TMDB)
tvstreamer-prep metadata

# Download this week's scheduled episodes
tvstreamer-prep download

# Verify that scheduled files exist locally
tvstreamer-prep verify

# Clean up obsolete (already-aired, unneeded) episodes
tvstreamer-prep delete

# Combined: delete + download + verify
tvstreamer-prep sync

# Combined: fetch + metadata + resolve duplicate broadcasts
tvstreamer-prep refresh
\```

Example crontab (sync daily at 03:00, refresh weekly on Mondays at 04:00):
\```
0 3 * * *   tvstreamer-prep sync
0 4 * * 1   tvstreamer-prep refresh
\```
